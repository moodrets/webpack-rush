include ../helpers/_vars
include ../helpers/_mixins

block vars
    -var pageTitle = 'Page title example'

<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= pageTitle
    body
        block content